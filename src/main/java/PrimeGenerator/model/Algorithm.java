// This Algorithm enum contains list of constants which can be used by the users to specify the algorithm through which
// they want to generate prime numbers.

package PrimeGenerator.model;

public enum Algorithm {
    ITERATIVE, SIEVE
}
